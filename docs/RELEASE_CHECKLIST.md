# リリースチェックリスト

## ベータ配布

- `swift test` が成功する
- `Scripts/build-app.sh` と `Scripts/verify-app.sh` が成功する
- ZIPとDMGを別ユーザー環境で展開し、初回設定が開く
- 表示、非表示、設定保存、再起動、検索、アプリ起動を確認する
- Aが右、Bが左に表示され、個別サイズ変更、吹き出し自動収納、クリック透過、常時前面切替が動作する
- AとBの吹き出しがそれぞれ対応するキャラクター位置から表示され、尻尾の向きと位置が正しい
- 通常の相談、検索、アプリ起動、エラー通知などの主要なコンシェルジュ応答はAが担当する
- Bは必要な場合だけ短い相づち、補足、感想、注意喚起を返し、重要情報がBの発言だけに含まれない
- Aのみ、AとB、Bのみの発言パターンで吹き出しの表示と自動収納が破綻しない
- AとBの吹き出しを同時表示してもキャ